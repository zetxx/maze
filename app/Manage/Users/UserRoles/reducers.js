import {actionList} from './actions'
import Immutable from 'immutable'

export const userRoles = (state = Immutable.Map(), action) => {
  switch (action.type) {
    case actionList.GET:
      break
  }
  return state
}
